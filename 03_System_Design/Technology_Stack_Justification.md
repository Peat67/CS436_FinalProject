# 🛠️ Technology Stack Justification: เว็บไซต์ BMI Tracker

เอกสารนี้อธิบายเหตุผลในการเลือกใช้เทคโนโลยีแต่ละส่วนของเว็บไซต์ BMI Tracker โดยเน้นที่ความเหมาะสมกับขนาดของโปรเจกต์ (MVP) และขอบเขตงาน (Session-Based Tracker)

---

## 💻 องค์ประกอบของ Technology Stack และเหตุผลในการเลือก (Rationale)

| องค์ประกอบเทคโนโลยี | เหตุผลในการเลือก (Rationale) |
| :--- | :--- |
| **Backend Framework:** Python / Flask | **ความเรียบง่ายและขนาด:** Flask เป็น Micro-framework ที่มีน้ำหนักเบาและง่ายต่อการเรียนรู้/ติดตั้ง เหมาะสำหรับโปรเจกต์ขนาดเล็ก (MVP) ที่มี Business Logic ไม่ซับซ้อน (Routing, Validation, Data Handling) Flask สามารถจัดการ **In-memory Data Storage** และการส่งข้อมูลไปยัง Frontend ได้อย่างรวดเร็ว |
| **Business Logic & Data Handling:** Python (Pure Python) | **ประสิทธิภาพของ Logic:** Python มีความสามารถในการจัดการตัวเลขและการคำนวณที่รวดเร็ว (เช่น การคำนวณ $BMI = \frac{kg}{m^2}$) และง่ายต่อการเขียน **Input Validation** และ Data Storage ในรูปแบบ List/Dictionary ชั่วคราว |
| **Templating Engine:** Jinja2 | **การผสานรวม:** Jinja2 เป็น Template Engine มาตรฐานของ Flask ทำให้การผสานโค้ด Python (Logic/Data) เข้ากับ HTML (Presentation) เป็นไปอย่างราบรื่นและมีประสิทธิภาพ (เช่น การใช้ `{% for record in records %}` เพื่อวนซ้ำข้อมูลในตาราง) |
| **Frontend Structure:** HTML5 | **มาตรฐาน:** ใช้ HTML5 เพื่อกำหนดโครงสร้างหน้าเว็บไซต์ และรองรับฟีเจอร์ฟอร์มอินพุตสมัยใหม่ (เช่น `<input type="date">`) |
| **Styling & Aesthetics:** CSS3 | **การออกแบบที่ยืดหยุ่น:** ใช้ CSS3 สำหรับการจัดรูปแบบและการสร้าง **Responsive Design** (เพื่อให้เว็บปรับขนาดได้ดีบนทุกหน้าจอ) และใช้ **Color Coding** เพื่อเน้นสถานะ BMI ซึ่งตรงตามความต้องการด้าน User Interface (UI) |
| **Data Visualization:** Chart.js (JavaScript Library) | **กราฟเส้นอย่างง่าย:** Chart.js เป็นไลบรารี JavaScript ที่เบาและใช้งานง่าย (**Client-Side Rendering**) เหมาะสำหรับ Basic Reporting ในรูปแบบกราฟเส้นโดยไม่ต้องอาศัยการประมวลผลกราฟบนเซิร์ฟเวอร์ (Python) ทำให้ลดความซับซ้อนของ Backend ได้มาก |

---

## 🔑 สรุปเหตุผลหลักในการเลือก Stack

* **ความเรียบง่าย (Simplicity):** Flask เป็นทางเลือกที่รวดเร็วที่สุดในการสร้าง API และจัดการ Routing โดยไม่ต้องมี **Boilerplate Code** มากมาย ทำให้เวลาในการพัฒนา (**Time-to-Market**) สั้นลง
* **ประสิทธิภาพ (Efficiency):** การใช้ Python จัดการ Business Logic และใช้ **Chart.js จัดการ Visualization บน Client-Side** ทำให้เกิดการแบ่งภาระงานอย่างเหมาะสม
* **ความเหมาะสมกับขอบเขต (Scope Fit):** เนื่องจากไม่มีระบบ Login/Database ถาวร การเลือกใช้ Flask และ **In-memory Storage** จึงเป็นทางเลือกที่ประหยัดทรัพยากรและตรงตาม **ข้อจำกัด (Constraints)** ของโครงการมากที่สุด
