# 🩺 BMI Tracker และประวัติสุขภาพเบื้องต้น (Session-Based Web Application)

เครื่องมือเว็บแอปพลิเคชันอย่างง่ายที่ช่วยให้ผู้ใช้สามารถคำนวณและติดตามดัชนีมวลกาย (BMI) ของตนเองได้อย่างรวดเร็วและเข้าใจง่าย โดยเน้นที่การแสดงผลแนวโน้มสุขภาพผ่านกราฟและรหัสสี 


---

## 1. แนะนำงาน (Project Overview)

### 1.1 วัตถุประสงค์ (Objectives)

* เพื่อพัฒนาเครื่องมือคำนวณค่า **BMI (Body Mass Index)** ที่แม่นยำตามมาตรฐาน
* เพื่อจัดเก็บประวัติการวัดน้ำหนักและส่วนสูง **ชั่วคราว (Session-Based)** เพื่อติดตามการเปลี่ยนแปลง
* เพื่อแสดงผลแนวโน้มของ BMI ผ่าน **กราฟเส้น** เพื่อช่วยให้ผู้ใช้เห็นภาพรวมของสุขภาพในช่วงเวลาหนึ่ง

### 1.2 เทคโนโลยีที่ใช้ (Technology Stack)

| องค์ประกอบ | เทคโนโลยีที่เลือกใช้ |
| :--- | :--- |
| **Backend** | Python 🐍 (**Flask** Micro-framework) |
| **Production Server** | Gunicorn (WSGI) |
| **Frontend/Templating** | HTML5, CSS3, **Jinja2** |
| **Data Visualization** | **Chart.js** (JavaScript Library) |
| **Deployment Platform** | Render (Web Service) |

### 1.3 คุณสมบัติหลัก (Key Features)

* **คำนวณ BMI:** คำนวณค่า BMI จากน้ำหนัก (kg) และส่วนสูง (cm) และแสดงผลเป็นทศนิยม 2 ตำแหน่ง
* **การจัดหมวดหมู่:** จัดประเภท BMI พร้อม **รหัสสี** (เช่น Healthy Weight: สีเขียว, Obesity: สีแดง)
* **การติดตามแนวโน้ม:** บันทึกข้อมูลในตาราง และแสดงแนวโน้มเป็น **กราฟเส้น**
* **Input Validation:** ตรวจสอบความถูกต้องของข้อมูล (เช่น ต้องเป็นตัวเลข, ค่าต้องไม่ติดลบ) ก่อนการคำนวณ

---

## 2. ขั้นตอนการใช้งาน (User Manual)

เว็บไซต์นี้ออกแบบมาให้ใช้งานง่ายใน 2 ขั้นตอนหลัก:

### 2.1 การบันทึกข้อมูลใหม่ (Input & Calculation)

1.  **เข้าสู่หน้าเว็บ:** เปิด URL ของเว็บไซต์ BMI Tracker ที่ Deploy บน Render
2.  **กรอกข้อมูลในส่วน "บันทึกข้อมูลใหม่":**
    * **วันที่:** เลือกวันที่ทำการวัด (ระบบจะตั้งค่าเป็นวันที่ปัจจุบันโดยอัตโนมัติ)
    * **น้ำหนัก (กิโลกรัม):** กรอกน้ำหนักตัวปัจจุบันเป็น kg (เช่น `70.5`)
    * **ส่วนสูง (เซนติเมตร):** กรอกส่วนสูงเป็น cm (เช่น `175.0`)
3.  **บันทึก:** คลิกปุ่ม **"บันทึกและคำนวณ BMI"**

> **หมายเหตุ:** หากข้อมูลไม่ถูกต้อง ระบบจะแสดงข้อความแจ้งเตือนสีแดง (Input Validation)

### 2.2 การตรวจสอบผลลัพธ์และการติดตาม (Reporting & Tracking)

หลังจากบันทึกข้อมูลแล้ว ผลลัพธ์จะปรากฏใน 2 ส่วนหลักทันที:

#### 📊 ส่วนที่ 1: กราฟเส้นแสดงแนวโน้ม (Trend Tracking)

* แสดงค่า BMI ที่เปลี่ยนแปลงไปตามวันที่บันทึก (**แกน X** คือวันที่, **แกน Y** คือค่า BMI)
* ช่วยให้คุณเห็นแนวโน้มว่า BMI ของคุณ **เพิ่มขึ้น หรือ ลดลง** อย่างไรในช่วงเวลาที่ผ่านมา

#### 📝 ส่วนที่ 2: ตารางประวัติการคำนวณ BMI (Detailed History)

* แสดงรายละเอียดการบันทึกแต่ละครั้ง (วันที่, น้ำหนัก, ส่วนสูง, BMI) โดยแสดง **แถวล่าสุดอยู่บนสุด**
* ตรวจสอบ "**หมวดหมู่**" (เช่น Underweight, Healthy Weight, Overweight) และรหัสสีเพื่อประเมินสถานะสุขภาพเบื้องต้นของคุณ

---

## ⚠️ ข้อจำกัดที่สำคัญ (Constraints)

* **ข้อมูลชั่วคราว:** ข้อมูลประวัติการคำนวณทั้งหมดจะถูกเก็บไว้ **ชั่วคราว (Session-Based)** ในหน่วยความจำของเซิร์ฟเวอร์หรือใน Session ของเบราว์เซอร์เท่านั้น
* **ข้อมูลจะหายไป:** ข้อมูลประวัติทั้งหมดจะถูกล้างและหายไป หาก **Server ถูกรีสตาร์ท** หรือ **Session หมดอายุ**
* **หน่วยวัด:** รองรับเฉพาะ **กิโลกรัม (kg)** และ **เซนติเมตร (cm)** เท่านั้น
