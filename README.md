# 📄 เอกสารขอบเขตโครงการ: เว็บไซต์คำนวณค่า BMI (Session-Based Tracker)

---

## 🚀 1. วัตถุประสงค์ (Objective)

จัดทำเครื่องมือออนไลน์ที่ **ง่ายต่อการใช้งาน** และ **แม่นยำ** สำหรับให้ผู้ใช้สามารถคำนวณและประเมินค่าดัชนีมวลกาย (BMI) ของตนเองได้อย่างรวดเร็ว พร้อมแสดงประวัติการคำนวณในเซสชันนั้น ๆ

---

## ✨ 2. ขอบเขตหลักและคุณสมบัติ (Core Scope and Features)

### 2.1 การคำนวณและประเมินผล
* **คำนวณค่า BMI:** ผู้ใช้สามารถป้อน **น้ำหนัก (กิโลกรัม)** และ **ส่วนสูง (เมตร)** เพื่อคำนวณค่า BMI
* **ให้ข้อมูลสุขภาพเบื้องต้น:** แสดงผลการประเมินสถานะน้ำหนักตัว (เช่น ผอมไป, น้ำหนักปกติ, ท้วม, อ้วน) ตามเกณฑ์มาตรฐานทันทีหลังการคำนวณ

### 2.2 การติดตามประวัติในเซสชัน (Session-Based Tracking)
* **บันทึกค่า BMI ชั่วคราว:** ทุกครั้งที่มีการคำนวณสำเร็จ ระบบจะบันทึกค่า BMI, น้ำหนัก, ส่วนสูง, และสถานะการประเมิน ไว้ใน **ประวัติเซสชันปัจจุบัน** (ข้อมูลนี้จะไม่ถูกบันทึกถาวรเมื่อผู้ใช้ปิดหน้าเว็บ)
* **การแสดงผลประวัติ (Visualizations):**
    * แสดง **แนวโน้ม BMI ในรูปแบบกราฟเส้น** สำหรับค่าที่บันทึกไว้ทั้งหมดในเซสชันนั้น
    * แสดงประวัติในรูปแบบ **ตาราง** ที่จัดประเภทและระบุสีตามเกณฑ์มาตรฐาน BMI

### 2.3 การแสดงผล (User Interface / UX)
* **รูปแบบที่ชัดเจน:** หน้าจอต้องแสดงผลลัพธ์การคำนวณปัจจุบันและประวัติการคำนวณในเซสชันด้วยการจัดรูปแบบที่อ่านง่ายและเข้าใจได้ทันที

---

## 🚫 3. ขอบเขตที่ไม่ได้ดำเนินการ (Out of Scope)

* **ระบบผู้ใช้งาน/ฐานข้อมูลถาวร:** ไม่มีระบบสมัครสมาชิก, เข้าสู่ระบบ, หรือการจัดเก็บข้อมูล BMI ของผู้ใช้แบบถาวรบนเซิร์ฟเวอร์
* **การจัดการข้อมูลแบบข้ามเซสชัน:** ข้อมูลการติดตาม BMI จะหายไปเมื่อผู้ใช้ปิดหรือรีเฟรชเบราว์เซอร์
* **คำแนะนำทางการแพทย์โดยละเอียด:** เว็บไซต์จะแสดงเพียงข้อมูลการประเมินสถานะน้ำหนักตามเกณฑ์มาตรฐานเท่านั้น จะไม่มีการให้คำแนะนำทางการแพทย์เฉพาะบุคคลหรือแผนอาหาร/การออกกำลังกายที่ซับซ้อน
