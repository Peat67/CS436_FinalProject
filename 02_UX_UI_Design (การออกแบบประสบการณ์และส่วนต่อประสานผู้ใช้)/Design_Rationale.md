# 🎨 Design Rationale: เว็บไซต์ BMI Tracker

เอกสารนี้อธิบายเหตุผลในการออกแบบ (Design Rationale) สำหรับ User Experience (UX) และ User Interface (UI) ของเว็บไซต์ BMI Tracker โดยเน้นที่ความ **ง่ายในการใช้งาน (Usability)** และ **ความชัดเจนของข้อมูล (Clarity)**

---

## 1. User Experience (UX) Rationale: ความง่ายในการใช้งาน

เป้าหมายหลักของ UX คือการทำให้ผู้ใช้สามารถคำนวณ BMI และดูประวัติสุขภาพได้อย่างรวดเร็วใน **หน้าเดียว (Single-Page Flow)**

| องค์ประกอบ | เหตุผลในการออกแบบ (Rationale) |
| :--- | :--- |
| **Single-Page Flow** | **ลดภาระทางความคิด (Cognitive Load):** ข้อมูลทุกอย่าง (ฟอร์ม, กราฟ, ตารางประวัติ) อยู่ในหน้าเดียว ไม่ต้องคลิกไปมาระหว่างหน้า ช่วยให้ผู้ใช้สามารถป้อนข้อมูลและดูผลลัพธ์ได้ทันที (**Efficiency**) |
| **Sequential Information Grouping** | **จัดวางลำดับข้อมูลตามธรรมชาติ:** Input Form (ป้อน) $\rightarrow$ Graph (สรุปแนวโน้ม) $\rightarrow$ Table (รายละเอียด) ทำให้ผู้ใช้สามารถประมวลผลข้อมูลจากภาพรวมสู่รายละเอียดได้ง่ายขึ้น |
| **Robust Input Validation** | **ป้องกันข้อผิดพลาด:** มีการตรวจสอบค่าในฝั่ง Python (Backend) และแจ้งเตือนด้วย **ข้อความสีแดงที่ชัดเจน (Error Prevention)** หากผู้ใช้กรอกข้อมูลที่ไม่ถูกต้องหรือนอกช่วงที่สมเหตุสมผล (เช่น ส่วนสูง $< 50\text{ cm}$), เพื่อให้แน่ใจว่าผลลัพธ์ที่ได้นั้นแม่นยำ |
| **POST-Redirect-GET (PRG)** | **ความน่าเชื่อถือ:** ใช้ PRG Pattern ใน Flask เพื่อ **ป้องกันการส่งข้อมูลซ้ำซ้อน (Duplicate Entry)** เมื่อผู้ใช้กดปุ่ม Refresh หลังจากการบันทึกข้อมูลสำเร็จ (**Reliability**) |

---

## 2. User Interface (UI) Rationale: Layout และการจัดสี

การออกแบบ UI ใช้หลักการ **Card-Based Layout** และ **Color Coding** เพื่อแยกส่วนประกอบและเน้นความสำคัญของข้อมูล 

[Image of BMI Calculator Website Interface]


| องค์ประกอบ | เหตุผลในการออกแบบ (Rationale) |
| :--- | :--- |
| **Layout (Full-Screen & Max-Width)** | เว็บไซต์ใช้พื้นที่เต็มหน้าจอ แต่จำกัดเนื้อหาหลักด้วย **Max-Width (1200px)** และจัดให้อยู่กึ่งกลาง (**Centering**) เพื่อให้ง่ายต่อการอ่านบนจอขนาดใหญ่ และยังคงความสวยงาม (**Aesthetics**) |
| **Card-Based Design (Sections)** | แต่ละส่วน (Input, Graph, Table) ถูกห่อหุ้มด้วย **Card Background (สีขาว)** และมี **เงา (Shadow)** ช่วยแยกกลุ่มข้อมูลออกจากพื้นหลังสีเทาอ่อน (`--background-color`) ทำให้องค์ประกอบดูโดดเด่นและเป็นระเบียบ (**Separation of Concerns**) |
| **Primary Color น้ำเงิน** | ใช้สีน้ำเงิน (Primary Color) สำหรับ ส่วนหัว (Header), ชื่อส่วน (Heading), และ เส้นกราฟ, เพื่อสร้างความรู้สึก **น่าเชื่อถือ (Trust)** และ **ความเป็นมืออาชีพ (Professionalism)** |
| **Secondary Color เขียว** | ใช้สีเขียว (Secondary Color) สำหรับ **ปุ่มบันทึก** และ สถานะ '**Healthy Weight**' เพื่อสร้างแรงจูงใจและเน้นย้ำถึงผลลัพธ์ด้านสุขภาพที่เป็นบวก (**Positive Reinforcement**) |
| **Color Coding (BMI Categories)** | ใช้สีในการจัดหมวดหมู่ BMI โดยอิงจากสัญญาณเตือน: **สีเขียว (ปกติ), สีเหลือง/ส้ม (เตือน/Overweight), สีแดง (อันตราย/Obesity)** การใช้สีช่วยให้ผู้ใช้ **แปลความหมายของผลลัพธ์ได้ทันที** โดยไม่ต้องอ่านข้อความทั้งหมด |
| **Form Grid Layout** | จัดเรียงช่องกรอกข้อมูลในรูปแบบ **Grid** ที่มีการปรับเปลี่ยนอัตโนมัติ (เช่น `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))`). ช่วยให้ฟอร์มดูเป็นระเบียบและยังคงใช้งานได้ดีบนหน้าจอขนาดเล็ก (**Responsiveness**) |

---

## 3. Reporting Rationale: กราฟและตาราง

การแสดงผลถูกออกแบบมาเพื่อความชัดเจนในการติดตามความคืบหน้า

| องค์ประกอบ | เหตุผลในการออกแบบ (Rationale) |
| :--- | :--- |
| **Table Display** | **ความละเอียดของข้อมูล:** ตารางแสดงตัวเลขที่แม่นยำ (น้ำหนัก, ส่วนสูง, BMI) และหมวดหมู่ในแต่ละวันที่บันทึก เป็นแหล่งข้อมูลที่ใช้ในการ **ตรวจสอบและยืนยันผลลัพธ์ (Data Precision)** |
| **Line Graph (Chart.js)** | **การติดตามแนวโน้ม (Trend Tracking):** การใช้กราฟเส้นเหมาะสำหรับการแสดงผลข้อมูลที่เปลี่ยนแปลงตามเวลา (**Time-Series Data**). ช่วยให้ผู้ใช้มองเห็นได้อย่างชัดเจนว่าค่า BMI มีแนวโน้ม **เพิ่มขึ้น, ลดลง, หรือคงที่** ตลอดระยะเวลาที่บันทึกไว้ (**Insight**) |
| **Data Synchronization** | ข้อมูลที่ป้อนในฟอร์มจะ **ปรากฏทันที** ทั้งในตารางและอัปเดตบนกราฟ (เมื่อมีข้อมูลเพียงพอ) ทำให้เกิดความสอดคล้องและต่อเนื่องในการใช้งาน (**Consistency**) |
